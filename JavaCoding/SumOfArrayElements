import java.util.stream.IntStream;
class Main {
    public static void main(String[] args) {
        int [] A = {1, 5, 7, -9, 4};
        int[] B = {2, 5, -1, 2};
        
       long sum = IntStream.of(A).sum() + IntStream.of(B).sum();
        System.out.println(sum);
    }
}